name: test
on: [push]
jobs:
  name: test job1
  job1:
    run:
      runs-on: [self-hosted, server]
      steps:
      - name: heck out repository code
      - run: |
          echo job1
  name: test job2
  job2:
    needs: job1
    run:
      runs-on: [self-hosted, server]
      steps:
      - name: job2
      - run: |
          echo job2

